spring:
  profiles:
    active: "${ACTIVE_PROFILE:mongodb,twilio}"
  graphql:
    websocket:
      path: /subscription
    schema:
      printer:
        enabled: true
    path: /graphql

  data:
    mongodb:
      uri: "${MONGODB_URI:}"
      database: "${MONGODB_DATABASE:}"
      username: "${MONGODB_USERNAME:}"
      password: "${MONGODB_PASSWORD:}"
      ca-cert-base64: "${MONGODB_CACERT_BASE64:}"

twilio:
  accountSid: "${TWILIO_ACCOUNT_SID:AC706cc305be2eabf5d2f150d4e9974b1a}"
  authToken: "${TWILIO_AUTH_TOKEN}"
  phoneNumber: "${TWILIO_PHONE:+18445230165}"

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info

logging:
  level:
    org.springframework.web: debug
    org.springframework.http: debug
    org.springframework.graphql: debug
    reactor.netty: debug
